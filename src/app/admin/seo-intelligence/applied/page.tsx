import { ComingSoonPanel } from "../ComingSoonPanel";

export default function Page() {
  return (
    <ComingSoonPanel
      title="Applied Changes"
      body="Version snapshots, impact tracking (before/after), and one-click rollback will appear here after the apply pipeline ships."
    />
  );
}
